# SQL Scripts

This folder contains SQL files used for database creation and querying.

Files:

- schema.sql: Database schema definition.
- queries.sql: Analytical SQL queries used for validation and reporting.

The scripts support loading and querying mutual fund data stored in SQLite.