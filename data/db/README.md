# Database

SQLite database generated using load_to_sqlite.py.

Database contains cleaned mutual fund datasets
used for analytical queries and reporting.

Note:
The .db file is excluded from Git tracking.
Use schema.sql and load_to_sqlite.py to recreate.